
- @projects.each do |project|
        = link_to (image_tag project.image.url(:medium)),project
        %h2= link_to project.name,project
        %p= time_ago_in_words(project.created_at)
        %p= truncate(project.description,length:50)


        = link_to "Creer Projet", new_project_path